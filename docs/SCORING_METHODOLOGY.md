# Scoring Methodology

The local screening score combines satellite subsidence magnitude, subsidence trend, groundwater extraction, construction activity, building density, soil susceptibility, rainfall anomaly, surface loading, and historical subsidence.

Weights are transparent in `app.py`. The result is a screening indicator rather than a prediction, engineering finding, or safety certification.
